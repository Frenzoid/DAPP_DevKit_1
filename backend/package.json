{
    "name": "dapp-template",
    "version": "1.0.0",
    "description": "A hardhat dapp template",
    "main": "index.js",
    "scripts": {
        "node": "hardhat node",
        "clean": "hardaht clean",
        "compile": "hardhat clean && hardhat compile",
        "deploy-local": "hardhat clean && hardhat compile && hardhat run ./scripts/deploy.js --network localhost --show-stack-traces",
        "deploy-mumbai": "hardhat clean && hardhat compile && hardhat run ./scripts/deploy.js --network mumbai --show-stack-traces",
        "deploy-goerli": "hardhat clean && hardhat compile && hardhat run ./scripts/deploy.js --network goerli --show-stack-traces",
        "test": "hardhat test"
    },
    "keywords": [],
    "author": "Frenzoid",
    "license": "ISC",
    "devDependencies": {
        "@nomiclabs/hardhat-ethers": "^2.0.0",
        "@nomiclabs/hardhat-waffle": "^2.0.0",
        "chai": "^4.2.0",
        "ethereum-waffle": "^3.0.0",
        "ethers": "^5.0.0",
        "hardhat": "^2.9.9"
    },
    "dependencies": {
        "@nomiclabs/hardhat-etherscan": "^3.0.3",
        "@openzeppelin/contracts": "^4.6.0",
        "dotenv": "^16.0.1",
        "fs": "^0.0.1-security"
    }
}
